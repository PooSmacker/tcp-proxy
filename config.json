{
    "settings": {
        "frontend_port": port you want the proxy to be run on
    },
    "proxied_host": {
        "general_settings": {
            "backend_ip": "your backend ip",
            "backend_port": backend port,
            "optional_note": "backend_port should be the port that the net backend is screened on"
        }
    }
}
